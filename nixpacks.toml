[phases.setup]
nixPkgs = ["pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]
cacheDirectories = [
  "node_modules",
  "apps/backend/node_modules",
  "apps/storefront/node_modules",
]

[phases.build]
cmds = [
  "pnpm turbo build --filter=medusa-2.0-boilerplate-backend",
  "cd apps/backend && init-backend && medusa db:migrate",
]
cacheDirectories = [
  # Turbo's own hash-based cache — skips rebuilding unchanged plugins
  ".turbo",
  # Per-plugin compiled output caches
  "packages/plugin-bundles/.medusa",
  "packages/plugin-email/.medusa",
  "packages/plugin-erp/.medusa",
  "packages/plugin-invoices/.medusa",
  "packages/plugin-loyalty/.medusa",
  "packages/plugin-preorder/.medusa",
  "packages/plugin-qr-marketing/.medusa",
  "packages/plugin-reviews/.medusa",
  "packages/plugin-shipstation/.medusa",
  "packages/plugin-subscription/.medusa",
]

[start]
cmd = "cd apps/backend/.medusa/server && medusa start --verbose"
